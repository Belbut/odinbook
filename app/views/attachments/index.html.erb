<%= render "profiles/profile_header", user: @user %>

<h1 class="title is-4">All user Photos</h1>

<div>
    <% @attachments.each do |attachment| %>
        <%= link_to(render_attachment_image(attachment), attachment.post ) %>
        <%# link_to "Delete", post_attachment_path(@post, attachment), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
    <% end %>
</div>
