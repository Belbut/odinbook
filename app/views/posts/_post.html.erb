<%# partial parameters: post[Post] ; optional: class_append[String] %>

<article class="post box <%= class_append if defined?(class_append) %>">
    <% if post.deleted? %>
        <%= render_deleted_content(post) %>
    <% else %>
        <%= render_visible_content(post) %>
    <% end %>
</article>
