<% precompute ||= {} %>

<div>
    <div class="fixed-grid has-3-cols">
        <div class="grid">
            <%= render_relationships_cards_with(collection, precompute: precompute) %>
        </div>
    </div>
</div>
