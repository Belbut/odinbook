services:
  - type: web
    name: odinbook-frankfurt
    runtime: ruby
    plan: free
    buildCommand: './bin/render-build.sh'
    startCommand: './bin/rails server'
    region: frankfurt      
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: odinbook-db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2
databases:
  - name: odinbook-db
    plan: free
    region: frankfurt      